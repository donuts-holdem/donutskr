# 2026-09-09 release checkpoint

## Validated class and club operations

- Unit/component/server-action tests: 27 files, 155 passing.
- ESLint and the Next.js 16.2.9 production build: passing.
- Isolated PostgreSQL 17.6: 56 migration, permission, RLS, concurrency, affiliation, attendance, XP and lifecycle checks passing.
- Browser checks: public pages at 390/768/1440 pixels and anonymous member/admin authorization gates are exercised separately.
- The schedule date and confirmation controls remain editable before submission. The server validates confirmation, dates and scoped permissions; the database checks exact preview revisions atomically.
- No production test accounts were created. Signed-in production browser flows and actual email delivery are not covered by these checks.

## Production database

Applied on 2026-09-09, after a custom-format PostgreSQL backup:

| Local migration | Remote version |
| --- | --- |
| 0025_independent_affiliations | 20260909083807 |
| 0026_class_club_operations | 20260909083808 |

Both files are now immutable applied migrations. Do not push their numbered filenames blindly against the timestamped remote history.

Backup: `/Users/seungmok/donuts-db-backups/2026-09-09T08-37-05.964Z-before-0025-0026/database-before-0025-0026.dump`.
Its private manifest includes the checksum, before/after row counts and content hashes. Storage metadata is included; object binaries are not duplicated by pg_dump.

Preserved: events 27, seasons 1, Auth users 1, Storage objects 63, buckets 1, site config 1, administrator allowlist 1, blind structures 6 and structure rows 190. All before/after hashes match. Signup remains closed and member profiles remain empty.

## Subsequent implementation

Successor enrollment and notifications, club meetings, and reviewed daily learning are separate additive slices. Resend is the confirmed notification provider. Real delivery requires `RESEND_API_KEY` and a verified `NOTIFICATIONS_FROM_EMAIL`; credentials must never be committed.

Validation artifacts: `/private/tmp/donuts-validation-0026-1788942795364`.

## Follow-up domain rollout

The successor/notification, club-meeting and learning slices subsequently passed
174 application tests in 34 files, ESLint, the production build, 72 isolated
PostgreSQL checks and 47 browser checks. Browser coverage includes 12 responsive
component-fixture checks, not real signed-in production sessions. Repeated form
labels were corrected to use unique React IDs, with a regression test.

Production migrations applied after a fresh backup:

| Local migration | Remote version |
| --- | --- |
| 0027_successors_notifications | 20260909093113 |
| 0028_club_meetings | 20260909093114 |
| 0029_reviewed_daily_learning | 20260909093115 |

These migrations are now immutable. Backup:
`/Users/seungmok/donuts-db-backups/2026-09-09T09-28-27.356Z-before-0027-0029/database-before-0027-0029.dump`.
All retained-data hashes still match. Fourteen new domain tables have RLS; direct
member question writes, private waitlist execution and email claiming are denied.
No production questions, meetings or test users were inserted. Signup remains
closed. Resend real delivery is explicitly deferred and off by default.

The explicit pg_cron meeting lifecycle script was applied. It handles waitlist
expiry and archival once per minute without an email HTTP scheduler. Details and
reproducible database validation are in [DOMAIN_EXTENSIONS.md](DOMAIN_EXTENSIONS.md).
